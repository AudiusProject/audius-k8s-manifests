---
# Source: audius-identity-service/templates/backend-deploy.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-backend
  namespace: default
spec:
  selector:
    matchLabels:
      release: identity
      tier: backend
  replicas: 1
  template:
    metadata:
      labels:
        release: identity
        tier: backend
      annotations:
        checksum/config: 000375b423f713f22443227e6bb771bfa0dafa4c0cfbce5a926fe7d41897eb89
    spec:
      
      containers:
      - name: identity-backend
        imagePullPolicy: Always
        
        image: audius/identity-service:96517412a87868f4ac3e2ea5ce255fb7e828731b
        
        envFrom:
        - configMapRef:
            name: identity-backend-cm
        
        ports:
        - containerPort: 7000
        livenessProbe:
          httpGet:
            path: /health_check
            port: 7000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /health_check
            port: 7000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 30
        
        command: ["sh", "-c", "exec node src/index.js"]
        
        
      


---
# Source: audius-identity-service/templates/cache-deploy.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-cache
  namespace: default
spec:
  selector:
    matchLabels:
      release: identity
      tier: cache
  replicas: 1
  template:
    metadata:
      labels:
        release: identity
        tier: cache
    spec:
      
      containers:
      - name: identity-cache
        image: redis:5.0.5
        ports:
        - containerPort: 6379


